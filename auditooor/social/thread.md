# X thread draft — post after DEMO.md exists

1/
auditooor v0.7 is a bounty hunter OS, not a contest auditor.

pack → unread-function wave → skeptics → fork-shaped PoC.
It will abort a fortress. Pashov will not.

https://github.com/Tejanadh/auditooor

2/
Self-contained for EVM. critfinds/sol/zk engines are optional.
If they are missing it still hunts (money-map, lifecycle, spec-divergence + MIT supporting fleet).

3/
Recorded demo, not a vibe:

OpenVault.drainTo is permissionless.
pack labelled it HUNT / permissionless out.
Foundry test: attacker steals 10 ETH, victim shares still credited.

`demo/DEMO.md` in the repo. [PASS] gas 56000.

4/
What we are not:
- a Medusa/Echidna suite generator (that's fizz)
- a calibrated in-wild hunter (ledger is empty until real programs)
- a Solana/Move depth engine (detect labels; adapters only)

5/
Install:

```
git clone https://github.com/Tejanadh/auditooor
bash auditooor/auditooor/install.sh
```

Then `/auditooor XRAY .` on something that actually pays.
