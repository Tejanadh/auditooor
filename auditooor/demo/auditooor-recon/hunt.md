# Hunt brief (machine)

**Posture:** `HUNT`

sloppy code + unguarded value — low-hanging AC/flow bugs first, then invariants

LLM: overlay protocol-specific invariant wording on `PROPERTIES.md`. Do not rewrite posture.
Next: `FORTRESS` → ABORT; `INVARIANT`/`HUNT`/`SEAM` → `/auditooor FUZZ` then fleet if still +EV.
